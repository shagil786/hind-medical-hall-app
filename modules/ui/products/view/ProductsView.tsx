import React from 'react'
import ProductsWrapper from '../components/wrapper'

const ProductsView = (props: any) => {
  return (
    <ProductsWrapper {...props} />
  )
}

export default ProductsView;
